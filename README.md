# PetitionApp Frontend

1. Clone the repo if needed at https://github.com/anthonyd03/petition-frontend

2. Install node.

3. Install dependencies: npm install

4. Run app: npm run dev (runs on port 3000)